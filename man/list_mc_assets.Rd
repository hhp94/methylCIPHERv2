% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_data.R
\name{list_mc_assets}
\alias{list_mc_assets}
\title{Clock Asset Inventory}
\usage{
list_mc_assets(groups = "all")
}
\arguments{
\item{groups}{A character vector. The asset groups to act on. One or more
of \code{"PCBrainAge"}, \code{"PCClocks"}, \code{"SystemsAge"} and \code{"Zhang2019"}, or
\code{"all"} for every group. Repeated values are ignored, and an empty vector
selects nothing. Default is \code{"all"}.}
}
\value{
A data.frame. One row for each requested group, with its clock
count, CpG count, asset size, download status, and the count and total
size of its superseded assets.
}
\description{
Lists the declared clock assets and their status in the assets
directory.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
list_mc_assets()
list_mc_assets("SystemsAge")
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=list_clocks]{list_clocks()}} for the clocks in the catalog.
\item \code{\link[=list_clock_tags]{list_clock_tags()}} for the tags a \code{clocks} value accepts.
\item \code{\link[=clock_cpgs]{clock_cpgs()}} for the CpGs a set of clocks needs.
\item \code{\link[=download_mc_assets]{download_mc_assets()}} to write the assets to disk.
\item \code{\link[=load_mc_assets]{load_mc_assets()}} to read the assets into memory.
\item \code{\link[=clear_mc_assets]{clear_mc_assets()}} to delete the assets from disk.
\item \code{\link[=get_mc_assets_dir]{get_mc_assets_dir()}} for the directory in effect.
\item \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose another directory.
}
}
