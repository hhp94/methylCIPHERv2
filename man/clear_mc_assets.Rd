% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_data.R
\name{clear_mc_assets}
\alias{clear_mc_assets}
\title{Clock Asset Removal}
\usage{
clear_mc_assets(groups = "all", ask = TRUE)
}
\arguments{
\item{groups}{A character vector. The asset groups to act on. One or more
of \code{"PCBrainAge"}, \code{"PCClocks"}, \code{"SystemsAge"} and \code{"Zhang2019"}, or
\code{"all"} for every group. Repeated values are ignored, and an empty vector
selects nothing. Default is \code{"all"}.}

\item{ask}{A boolean. Asks for consent before a download or a delete.
Default is \code{TRUE}. Pass \code{FALSE} to download or delete without asking, in
a non-interactive session.}
}
\value{
A character vector. The path to each deleted asset file, returned
invisibly. Empty when no file was removed.
}
\description{
Deletes the clock assets for one or more groups from the assets
directory.
}
\details{
\code{clear_mc_assets()} removes both the currently declared assets and every
superseded asset left behind by an earlier sync, for the requested groups.
It asks for consent before deleting, refuses in a non-interactive
session, and treats \code{ask = FALSE} as consent to proceed without asking.
If the assets directory holds no asset for the requested groups, no file
is removed.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
clear_mc_assets("SystemsAge")
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=list_mc_assets]{list_mc_assets()}} for the assets on disk and their size.
\item \code{\link[=download_mc_assets]{download_mc_assets()}} to write the assets to disk.
\item \code{\link[=load_mc_assets]{load_mc_assets()}} to read the assets into memory.
\item \code{\link[=get_mc_assets_dir]{get_mc_assets_dir()}} for the directory in effect.
\item \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose another directory.
}
}
