% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_data.R
\name{download_mc_assets}
\alias{download_mc_assets}
\title{Clock Asset Downloads}
\usage{
download_mc_assets(groups = "all", ask = TRUE)
}
\arguments{
\item{groups}{A character vector. The asset groups to act on. One or more
of \code{"PCBrainAge"}, \code{"PCClocks"}, \code{"SystemsAge"} and \code{"Zhang2019"}, or
\code{"all"} for every group. Repeated values are ignored, and an empty vector
selects nothing. Default is \code{"all"}.}

\item{ask}{A boolean. Asks for consent before a download or a delete.
Default is \code{TRUE}. Pass \code{FALSE} to download or delete without asking, in
a non-interactive session.}
}
\value{
A character vector. The path to each requested asset file, named
by group id. Returned invisibly.
}
\description{
Downloads the clock assets for one or more groups into the assets
directory.
}
\details{
Only an asset that is missing from the assets directory is fetched.
\code{download_mc_assets()} asks for consent before a download, refuses in a
non-interactive session, and treats \code{ask = FALSE} as consent to proceed
without asking.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
download_mc_assets("SystemsAge")
download_mc_assets("SystemsAge", ask = FALSE)
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=list_mc_assets]{list_mc_assets()}} for the assets already on disk.
\item \code{\link[=load_mc_assets]{load_mc_assets()}} to read the assets into memory.
\item \code{\link[=clear_mc_assets]{clear_mc_assets()}} to delete the assets from disk.
\item \code{\link[=get_mc_assets_dir]{get_mc_assets_dir()}} for the directory in effect.
\item \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose another directory.
}
}
