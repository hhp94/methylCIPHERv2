% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_result.R
\name{print.mc_result}
\alias{print.mc_result}
\title{Print Method For An mc_result Object}
\usage{
\method{print}{mc_result}(x, n = 6, p = 6, ...)
}
\arguments{
\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}

\item{n}{A single whole number. The number of sample rows to print.
Default is \code{6}.}

\item{p}{A single whole number. The number of clock columns to print for
the scores table. Default is \code{6}.}

\item{...}{Not used.}
}
\value{
An \code{mc_result} object. Returns \code{x}, invisibly, after printing it.
}
\description{
Prints the scores and the \code{pheno} table for an \code{mc_result} object.
}
\details{
The output lists batch labels only when \code{x} spans more than one
\code{mc_batch_id}.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim <- sim_DNAm(clocks, n = 20)
res <- calc_clocks(sim[["DNAm"]], clocks)
print(res, n = 3, p = 2)

}
