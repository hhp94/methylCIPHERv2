% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_clocks.R
\name{list_clocks}
\alias{list_clocks}
\title{Epigenetic Clock Catalog}
\usage{
list_clocks(group = NULL, tag = NULL, pattern = NULL, all_columns = FALSE)
}
\arguments{
\item{group}{A character vector. Keeps only the clocks in these groups.
Default is \code{NULL}, which keeps every group.}

\item{tag}{A character vector. Keeps only the clocks that carry one of
these tags. Default is \code{NULL}, which applies no tag filter.}

\item{pattern}{A string. A regular expression matched against the clock id
and the group id. Default is \code{NULL}, which applies no pattern filter.}

\item{all_columns}{A boolean. Returns every column, including the ones the
frame leaves out by default. Default is \code{FALSE}.}
}
\value{
A data.frame. One row for each clock in the catalog, including a
clock that scores only as part of another clock.
}
\description{
Lists the clocks in the catalog, with the group, tags, and token to
request each one.
}
\details{
Valid values for \code{tag} are the names of \code{\link[=list_clock_tags]{list_clock_tags()}}.

\code{request_as} names the token to pass to \code{clocks} in \code{\link[=calc_clocks]{calc_clocks()}}. It
differs from \code{clock_id} for a clock that only another clock can request.
\code{covariates} names the \code{\link[=calc_clocks]{calc_clocks()}} \code{pheno} columns a clock needs, and
\code{external} is \code{TRUE} for a clock whose weights are a download.

\code{all_columns = TRUE} adds four more columns.
\itemize{
\item \code{callable} is \code{FALSE} for a clock that only another clock can request.
\item \code{group_size} counts the callable clocks a group token expands to.
\item \code{batch_dependent} is \code{TRUE} for a clock whose score depends on the other
samples scored with it.
\item \code{normalize} names the background normalization a clock gets, and is
empty for a clock that gets none. \code{"bmiq"} is optional, and you turn it
on with the \code{normalize} argument of \code{\link[=calc_clocks]{calc_clocks()}}. \code{"quantile"} is
part of the clock's definition and is always applied.
}
}
\examples{
list_clocks(pattern = "^Horvath")
nrow(list_clocks(tag = "mortality"))
list_clocks(group = "Dunedin", all_columns = TRUE)

}
\seealso{
\itemize{
\item \code{\link[=list_clock_tags]{list_clock_tags()}} for the tags a \code{tag} value accepts.
\item \code{\link[=clock_cpgs]{clock_cpgs()}} for the CpGs a set of clocks needs.
\item \code{\link[=list_mc_assets]{list_mc_assets()}} for the assets an external clock needs.
}
}
