% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_data.R
\name{load_mc_assets}
\alias{load_mc_assets}
\title{Loaded Clock Assets}
\usage{
load_mc_assets(groups, ext_data = NULL, ask = TRUE)
}
\arguments{
\item{groups}{A character vector. The asset groups to load. One or more of
\code{"PCBrainAge"}, \code{"PCClocks"}, \code{"SystemsAge"} and \code{"Zhang2019"}, or
\code{"all"} for every group. Repeated values are ignored, and an empty vector
loads nothing.}

\item{ext_data}{A string. The path to the directory that holds the clock
assets. Default is \code{NULL}, which uses the assets directory.}

\item{ask}{A boolean. Asks for confirmation before the assets directory
changes. Default is \code{TRUE}. Pass \code{FALSE} to continue without asking, in a
non-interactive session.}
}
\value{
An \code{mc_assets} object. It holds the loaded asset for each
requested group, in the order of \code{groups}.
}
\description{
Reads the assets for one or more clock groups into memory.
}
\details{
\code{load_mc_assets()} asks for confirmation before it downloads an asset that
is not already in the assets directory. It refuses to download in a
non-interactive session. \code{ask = FALSE} confirms the download in advance.

An asset in \code{ext_data} for a group that was not requested is not used, and
\code{load_mc_assets()} warns about it.
}
\section{The assets directory}{

Four clock groups keep their weights in downloadable assets, outside the
package. \code{ext_data} says where to read them from, and accepts three forms.
\itemize{
\item \code{NULL} reads from the assets directory, and downloads any asset that is
missing. Use \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose that directory.
\item A path reads only that directory, and never downloads. A missing asset
is an error.
\item Assets already in memory from \code{\link[=load_mc_assets]{load_mc_assets()}} are used directly.
}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
assets <- load_mc_assets("SystemsAge")
names(assets)
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=list_mc_assets]{list_mc_assets()}} for the assets already on disk.
\item \code{\link[=download_mc_assets]{download_mc_assets()}} to write the assets to disk.
\item \code{\link[=clear_mc_assets]{clear_mc_assets()}} to delete the assets from disk.
\item \code{\link[=get_mc_assets_dir]{get_mc_assets_dir()}} for the directory in effect.
\item \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose another directory.
}
}
