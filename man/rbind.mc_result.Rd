% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind.R
\name{rbind.mc_result}
\alias{rbind.mc_result}
\title{Combined Batches Of Scores}
\usage{
\method{rbind}{mc_result}(..., deparse.level = 1)
}
\arguments{
\item{...}{Two or more \code{mc_result} objects.}

\item{deparse.level}{A single whole number. Not used by this method.
Default is \code{1}.}
}
\value{
An \code{mc_result} object. It holds the stacked scores, \code{pheno}, and
\code{coverage} of every input, under one \code{mc_batch_id} label for each.
}
\description{
Stacks two or more outputs from \code{\link[=calc_clocks]{calc_clocks()}} runs into one object of
multiple batches.
}
\details{
Each input must use disjoint sample ids, the same scored clocks, the
same \code{pheno_id}, and the same \code{normalize} setting. \code{rbind()} stops when
any of those differ between inputs.

The combined value gets one \code{mc_batch_id} label for each input. A clock
that depends on sample-wise information, such as a z-score, keeps the
value each input calculated on its own samples. Call
\code{\link[=refinalize_clocks]{refinalize_clocks()}} to calculate it again from every sample in the
combined value.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim1 <- sim_DNAm(clocks, n = 10)
sim2 <- sim_DNAm(clocks, n = 10, suffix = "b")

res1 <- calc_clocks(sim1[["DNAm"]], clocks)
res2 <- calc_clocks(sim2[["DNAm"]], clocks)

combined <- rbind(res1, res2)
combined

}
\seealso{
\itemize{
\item \code{\link[=refinalize_clocks]{refinalize_clocks()}} for a cross-sample score recomputed after a bind.
\item \code{\link[=as.data.frame.mc_result]{as.data.frame.mc_result()}} for the scores as a data.frame.
\item \code{\link[=as.matrix.mc_result]{as.matrix.mc_result()}} for the scores as a numeric matrix.
}
}
