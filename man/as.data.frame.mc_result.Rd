% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_accel.R
\name{as.data.frame.mc_result}
\alias{as.data.frame.mc_result}
\title{Data Frame Method For An mc_result Object}
\usage{
\method{as.data.frame}{mc_result}(x, row.names = NULL, optional = FALSE, ..., long = TRUE)
}
\arguments{
\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}

\item{row.names}{A character vector. Not used by this method. Default is
\code{NULL}.}

\item{optional}{A boolean. Not used by this method. Default is
\code{FALSE}.}

\item{...}{Not used.}

\item{long}{A boolean. Returns one row for each sample and clock when
\code{TRUE}, and one row for each sample, with one column for each clock, when
\code{FALSE}. Default is \code{TRUE}.}
}
\value{
A data.frame. In long form, one row for each sample and clock,
with the score and, when \code{x} holds more than one batch, an
\code{mc_batch_id} column. In wide form, one row for each sample, with one
column for each clock.
}
\description{
Converts the \code{\link[=calc_clocks]{calc_clocks()}} output to a data.frame containing just the
clocks, in long or wide format.
}
\section{Clocks that use all the samples}{

Some clocks depend on information from all the samples, such as a z-score.
When \code{x} holds more than one batch, these clocks take their value from
every sample in \code{x}, and not from one batch alone. This is the same
calculation as \code{\link[=refinalize_clocks]{refinalize_clocks()}}.
}

\examples{
clocks <- c("Horvath1", "Hannum")
sim <- sim_DNAm(clocks, n = 20)
res <- calc_clocks(sim[["DNAm"]], clocks)

head(as.data.frame(res))
head(as.data.frame(res, long = FALSE))

}
\seealso{
\itemize{
\item \code{\link[=as.matrix.mc_result]{as.matrix.mc_result()}} for the scores as a numeric matrix.
\item \code{\link[=rbind.mc_result]{rbind.mc_result()}} for two runs combined into one object.
\item \code{\link[=refinalize_clocks]{refinalize_clocks()}} for a cross-sample score recomputed after a bind.
}
}
