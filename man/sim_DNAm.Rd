% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim_DNAm.R
\name{sim_DNAm}
\alias{sim_DNAm}
\title{Simulated Methylation Data}
\usage{
sim_DNAm(
  clocks,
  n = 10,
  Age = FALSE,
  Female = FALSE,
  remove = 0,
  normalize = NULL,
  ext_data = NULL,
  ask = TRUE,
  suffix = NULL
)
}
\arguments{
\item{clocks}{A character vector. The clocks to score, named by clock id,
group id, or tag. See \code{\link[=list_clocks]{list_clocks()}}.}

\item{n}{A single whole number. The number of samples to simulate. Default
is \code{10}.}

\item{Age}{A boolean. Adds an \code{Age} column to \code{pheno}, drawn from a
normal distribution. Default is \code{FALSE}.}

\item{Female}{A boolean. Adds a \code{Female} column to \code{pheno}, with
about half the samples set to \code{1}. Default is \code{FALSE}.}

\item{remove}{A single whole number. The number of CpGs to drop at random
from the simulated panel. Default is \code{0}.}

\item{normalize}{A named logical vector. Turns background normalization on
for the clocks that support it. Default is \code{NULL}, which leaves the
optional schemes off.}

\item{ext_data}{A string. The path to the directory that holds the clock
assets. Default is \code{NULL}, which uses the assets directory.}

\item{ask}{A boolean. Asks for confirmation before the assets directory
changes. Default is \code{TRUE}. Pass \code{FALSE} to continue without asking, in a
non-interactive session.}

\item{suffix}{A string. Appended to every sample id, so two simulated
matrices stay distinct. Default is \code{NULL}, which leaves the ids as
given.}
}
\value{
An \code{mc_sim} object. It holds the simulated \code{DNAm} matrix, the
matching \code{pheno} data.frame, the \code{clocks} argument as given, and the
\code{suffix}, which is \code{NULL} when no suffix was set.
}
\description{
Builds a random beta matrix and a matching \code{pheno} data.frame for a set of
clocks.
}
\section{The assets directory}{

Four clock groups keep their weights in downloadable assets, outside the
package. \code{ext_data} says where to read them from, and accepts three forms.
\itemize{
\item \code{NULL} reads from the assets directory, and downloads any asset that is
missing. Use \code{\link[=set_mc_assets_dir]{set_mc_assets_dir()}} to choose that directory.
\item A path reads only that directory, and never downloads. A missing asset
is an error.
\item Assets already in memory from \code{\link[=load_mc_assets]{load_mc_assets()}} are used directly.
}
}

\examples{
sim <- sim_DNAm(c("Horvath1", "Hannum"), n = 10, Age = TRUE, Female = TRUE)
dim(sim[["DNAm"]])
head(sim[["pheno"]])

}
