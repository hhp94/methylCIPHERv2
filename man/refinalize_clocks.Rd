% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bind.R
\name{refinalize_clocks}
\alias{refinalize_clocks}
\title{Scores Recomputed From All Samples}
\usage{
refinalize_clocks(x)
}
\arguments{
\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}
}
\value{
An \code{mc_result} object. The same as \code{x}, with any score that is
computed from all its samples together computed again from every
sample in \code{x}.
}
\description{
Recalculates every clock that depends on sample-wise information, such as
a z-score, using the samples now in \code{x}.
}
\details{
A clock of that kind is calculated once from every sample in a
\code{calc_clocks()} call, not one sample at a time. After \code{rbind()} combines
several such values, each score still holds the value its own input
calculated. \code{refinalize_clocks()} calculates it again from every sample in
\code{x}.

\code{refinalize_clocks()} changes nothing, and returns \code{x} unchanged, when
\code{x} holds no clock of that kind.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim1 <- sim_DNAm(clocks, n = 10)
sim2 <- sim_DNAm(clocks, n = 10, suffix = "b")

res1 <- calc_clocks(sim1[["DNAm"]], clocks)
res2 <- calc_clocks(sim2[["DNAm"]], clocks)
combined <- rbind(res1, res2)

# a no-op here, because neither Horvath1 nor Hannum is scored from all
# samples together
refinalize_clocks(combined)

}
\seealso{
\itemize{
\item \code{\link[=rbind.mc_result]{rbind.mc_result()}} for two runs combined into one object.
\item \code{\link[=as.data.frame.mc_result]{as.data.frame.mc_result()}} for the scores as a data.frame.
\item \code{\link[=as.matrix.mc_result]{as.matrix.mc_result()}} for the scores as a numeric matrix.
}
}
