% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_result.R
\name{as.matrix.mc_result}
\alias{as.matrix.mc_result}
\title{Matrix Method For An mc_result Object}
\usage{
\method{as.matrix}{mc_result}(x, ...)
}
\arguments{
\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}

\item{...}{Not used.}
}
\value{
A numeric matrix. The scores, with samples in the rows and
clocks in the columns.
}
\description{
Converts the \code{\link[=calc_clocks]{calc_clocks()}} output to a matrix containing just the clocks.
}
\details{
This function recalculates any clock that depends on sample-wise
information, such as a z-score, from all the available samples when \code{x}
holds more than one batch. This is the same calculation as
\code{\link[=refinalize_clocks]{refinalize_clocks()}}.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim <- sim_DNAm(clocks, n = 10)
res <- calc_clocks(sim[["DNAm"]], clocks)
as.matrix(res)

}
\seealso{
\itemize{
\item \code{\link[=as.data.frame.mc_result]{as.data.frame.mc_result()}} for the scores as a data.frame.
\item \code{\link[=rbind.mc_result]{rbind.mc_result()}} for two runs combined into one object.
\item \code{\link[=refinalize_clocks]{refinalize_clocks()}} for a cross-sample score recomputed after a bind.
}
}
