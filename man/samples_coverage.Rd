% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coverage_report.R
\name{samples_coverage}
\alias{samples_coverage}
\title{Sample Coverage Counts}
\usage{
samples_coverage(x)
}
\arguments{
\item{x}{An \code{mc_result} object. The value returned by \code{\link[=calc_clocks]{calc_clocks()}}.}
}
\value{
A data.frame. One row for each sample, clock, and panel, with
\code{n_observed}, \code{n_needed}, \code{coverage}, and, when \code{x} holds more than
one batch, \code{mc_batch_id}.
}
\description{
Reports each sample's CpG coverage for every clock in \code{x}, one row for
each sample, clock, and panel.
}
\details{
Only the clocks in the returned scores of \code{x} get a row. A clock that
scores as part of another clock gets none. A clock assembled only from
other clocks' scores gets none. A
clock scored separately for each sex has no row for a sample outside
the sex it scored.

A clock that normalizes has a second row for each sample, under
\code{panel = "norm"}, for the panel used to normalize it.

\code{samples_coverage()} warns when a row's \code{coverage} is under the
strictest \code{min_samples_coverage} value used to score \code{x}. The
\code{mc_batch_id} column appears only when \code{x} holds more than one batch.
}
\examples{
clocks <- c("Horvath1", "Hannum")
sim <- sim_DNAm(clocks, n = 20)
res <- calc_clocks(sim[["DNAm"]], clocks)

head(samples_coverage(res))

}
\seealso{
\code{\link[=clocks_coverage]{clocks_coverage()}} for the same panels counted for each clock.
}
