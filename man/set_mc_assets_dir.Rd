% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_data.R
\name{set_mc_assets_dir}
\alias{set_mc_assets_dir}
\title{Assets Directory Override}
\usage{
set_mc_assets_dir(path = NULL)
}
\arguments{
\item{path}{A string. The new assets directory. Default is \code{NULL}, which
clears the override.}
}
\value{
A string. The override that was in place, returned invisibly, or
\code{NULL} when no override was set.
}
\description{
Sets or clears the directory this session uses for clock assets.
}
\details{
\code{set_mc_assets_dir()} creates the directory if it does not exist yet. It
stops if the directory cannot be created or is not writable. Clearing the
override falls back to the \code{mc.assets_dir} option, the \code{MC_ASSETS_DIR}
environment variable, or the default cache directory, in that order.

The return value restores the previous state exactly, in the manner of
\code{\link[=setwd]{setwd()}}. It is the override that was in place, and it is \code{NULL} when no
override was set. Passing \code{NULL} back clears the override, rather than
pinning it to the directory that happened to be in effect.

For the directory in effect, which is always a path, call
\code{\link[=get_mc_assets_dir]{get_mc_assets_dir()}}.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
old <- set_mc_assets_dir(tempdir())
get_mc_assets_dir()
set_mc_assets_dir(old)
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=get_mc_assets_dir]{get_mc_assets_dir()}} for the directory in effect.
\item \code{\link[=list_mc_assets]{list_mc_assets()}} for the assets in the directory.
\item \code{\link[=download_mc_assets]{download_mc_assets()}} to write the assets to disk.
\item \code{\link[=load_mc_assets]{load_mc_assets()}} to read the assets into memory.
\item \code{\link[=clear_mc_assets]{clear_mc_assets()}} to delete the assets from disk.
}
}
